@isTest
public with sharing class LeadRegionNotificationServiceTest {
    @isTest
    static void testCodeCoverage() {
        Test.startTest();
        LeadRegionNotificationService.codeCoverage();
        Test.stopTest();
    }
}